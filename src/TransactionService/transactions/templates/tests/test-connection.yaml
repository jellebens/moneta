apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transactionsvc.fullname" . }}-test-connection"
  labels:
    {{- include "transactionsvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transactionsvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
