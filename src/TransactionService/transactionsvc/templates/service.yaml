apiVersion: v1
kind: Service
metadata:
  name: {{ include "transactionsvc.fullname" . }}
  labels:
    {{- include "transactionsvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transactionsvc.selectorLabels" . | nindent 4 }}
